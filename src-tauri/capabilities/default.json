{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the settings window (Profiles and Logs)",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "autostart:default",
    "dialog:default"
  ]
}
